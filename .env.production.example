# Production Environment Variables Example
# EC2 + CloudFront + ACM + RDS 배포 환경용
# 사용법: backend/.env 파일로 복사하여 실제 값으로 수정

# Application
APP_NAME=blog-mine-backend
APP_VERSION=0.0.1
NODE_ENV=production

# Server
PORT=9706

# RDS Database (외부 PostgreSQL)
DB_HOST=your-rds-instance.xxxxxx.ap-northeast-2.rds.amazonaws.com
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-secure-rds-password
DB_DATABASE=blog_mine_production
DB_SYNCHRONIZE=false
DB_LOGGING=false
DB_SSL=true

# Prisma Database URL (RDS PostgreSQL)
DATABASE_URL="postgresql://postgres:your-secure-rds-password@your-rds-instance.xxxxxx.ap-northeast-2.rds.amazonaws.com:5432/blog_mine_production?schema=public&sslmode=require"

# JWT (IMPORTANT: Use strong secret in production)
JWT_SECRET=your-strong-production-secret-change-this-min-32-chars
JWT_EXPIRES_IN=1d

# CORS (프론트엔드 도메인)
CORS_ORIGIN=https://blogmine.ai.kr

# Logging
LOG_LEVEL=warn

# Swagger (프로덕션에서는 비활성화)
SWAGGER_ENABLED=false

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# OpenAI
OPENAI_API_KEY=sk-your-production-openai-api-key
OPENAI_SUMMARY_MODEL=gpt-4o-mini
OPENAI_GENERATION_MODEL=gpt-4o

# Naver API
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret

# Email (Production SMTP)
EMAIL_SMTP=smtp.yourdomain.com
EMAIL_USER=noreply@yourdomain.com
EMAIL_PASS=your-email-password

# Nicepay (Production)
NICEPAY_UNAUTH_MID=your-production-nicepay-mid
NICEPAY_UNAUTH_KEY=your-production-nicepay-key
